{
  "name": "event-blog",
  "version": "1.0.0",
  "description": "Blog API server",
  "license": "MIT",
  "scripts": {
    "start": "json-server --port 3001 --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.12.0"
  }
}
